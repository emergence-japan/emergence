# 実装プラン: 運用安定化 ＆ コンテンツ拡充（2026/02/07）

## 1. 完了済みタスク（2026/02/07）

### フォーム送信システムの修正
- [x] **フロントエンドの修正**: 
    - `ContactForm.tsx`, `school-application/page.tsx` において、送信方式を JSON から `URLSearchParams` (フォーム形式) に変更。
    - Vercel の環境変数読み込み問題を回避するため、GAS のエンドポイント URL をハードコードし、確実な動作を担保。
    - 送信時のデバッグログを追加。
- [x] **GAS (Google Apps Script) の修正**: 
    - `gas-script.js`, `gas-school.js` を更新し、`e.parameter` (フォーム形式) を受け取れる「万能版」へアップグレード。
    - `openById` によるスプレッドシートの直接指定を行い、書き込み先を固定。
    - 空行挿入バグの解消。
    - 管理者通知 ＆ 利用者への自動返信メール（スクール用詳細文面含む）の実装を完了。
- [x] **本番反映**: 
    - GitHub へのプッシュおよび Vercel での再デプロイを行い、本番サイトでの正常動作を確認。
- [x] **独自ドメインの移行**: 
    - 独自ドメイン `emergence-japan.com` を Vercel へ紐付け完了。Google Workspace (Gmail) との共存設定も正常。

### コンテンツの更新
- [x] **Newsセクション**: 
    - 代表・浜田陽介の出版情報（2025年10月）を1件のみ表示するように修正。

## 2. 残り作業（次回以降）

### 運用 ＆ コンテンツ管理
- [x] **News運用の確立**: 
    - 代表の出版情報やメディア掲載を、非エンジニアでも簡単に更新できる仕組みの構築（スプレッドシート連携）。
    - ニュース専用のスプレッドシートおよび専用GAS（gas-news.js）を新設し、運用安定性を確保。
    - 運用の手間を最小限にしつつ、最新情報を発信し続けられる体制を構築。
